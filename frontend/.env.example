# Em desenvolvimento, deixe vazio para usar o proxy do Vite (vite.config.js → localhost:3000).
# Em produção (build estático), defina a URL base da API, ex.:
# VITE_API_URL=http://localhost:3000
VITE_API_URL=
